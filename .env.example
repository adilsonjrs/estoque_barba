# Environment Configuration for Estoque Barba
# Copy this file to .env and configure your settings

# Database Configuration
DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=root
DB_NAME=estoque_barba

# Security Configuration
SESSION_LIFETIME=3600
CSRF_TOKEN_EXPIRY=7200

# Application Settings
APP_ENV=development
DEBUG_MODE=false

# IMPORTANT SECURITY NOTES:
# 1. Never commit the .env file to version control
# 2. Set proper file permissions (600) on the .env file
# 3. In production, use strong database passwords
# 4. Consider using SSL/TLS for database connections
# 5. Enable HTTPS in production environments